package com.icp.worstfilmapi.utils;
import com.icp.worstfilmapi.dto.request.FilmDTO;
import com.icp.worstfilmapi.entity.Film;

import java.time.LocalDate;
import java.util.Collections;

public class PersonUtils {

    /*private static final String FIRST_NAME = "Rodrigo";
    private static final String LAST_NAME = "Peleias";
    private static final String CPF_NUMBER = "369.333.878-79";
    private static final long PERSON_ID = 1L;
    public static final LocalDate BIRTH_DATE = LocalDate.of(2010, 10, 1);

    public static FilmDTO createFakeDTO() {
        return FilmDTO.builder()
                .fistName(FIRST_NAME)
                .lastName(LAST_NAME)
                .cpf(CPF_NUMBER)
                .birthDate("04-04-2010")
                .phones(Collections.singletonList(PhoneUtils.createFakeDTO()))
                .build();
    }

    public static Film createFakeEntity() {
        return Film.builder()
                .id(PERSON_ID)
                .fistName(FIRST_NAME)
                .lastName(LAST_NAME)
                .cpf(CPF_NUMBER)
                .birthDate(BIRTH_DATE)
                .producers(Collections.singletonList(PhoneUtils.createFakeEntity()))
                .build();
    }

*/


}
