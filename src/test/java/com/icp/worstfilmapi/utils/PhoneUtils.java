package com.icp.worstfilmapi.utils;

import com.icp.worstfilmapi.dto.request.ProducerDTO;
import com.icp.worstfilmapi.entity.Producer;

public class PhoneUtils {

    /*private static final String PHONE_NUMBER = "1199999-9999";
    private static final PhoneType PHONE_TYPE = PhoneType.MOBILE;
    private static final long PHONE_ID = 1L;

    public static ProducerDTO createFakeDTO() {
        return ProducerDTO.builder()
                .number(PHONE_NUMBER)
                .type(PHONE_TYPE)
                .build();
    }

    public static Producer createFakeEntity() {
        return Producer.builder()
                .id(PHONE_ID)
                .number(PHONE_NUMBER)
                .type(PHONE_TYPE)
                .build();
    }*/
}